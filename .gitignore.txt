# Python 缓存文件（Git会忽略这些无用的缓存）
__pycache__/
*.pyc
*.pyo
*.pyd

# Python 虚拟环境（避免上传本地的虚拟环境文件夹）
venv/
env/
ENV/

# 编辑器配置文件（只保留代码，不上传编辑器的个性化设置）
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# 系统自动生成的无用文件
.DS_Store       # Mac系统
Thumbs.db       # Windows系统
*.log           # 日志文件